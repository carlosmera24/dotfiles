return {
    -- Reemplazado por mini.indentscope
    -- {
    --     'yggdroot/indentline',
    --     config = function()
    --         -- Deshabilitado para archivos JSON y Mardown
    --         vim.g.vim_json_conceal = 0
    --         vim.g.markdown_syntax_conceal = 0
    --         -- Visualizar comillas(quotes) dobles en modo visual y edición
    --         -- ya que con nvim-treesitter json no se visualizan a no ser que se ingrese en modo VISUAL.
    --         vim.cmd([[
    --             autocmd FileType json let g:indentLine_concealcursor = 'v'
    --         ]])
    --     end
    -- }
}
